// Common length/size limits used across UI features

export const MAX_COMMENT_LENGTH = 500;
export const MAX_AUTHOR_NAME_LENGTH = 50;

// Editor
export const EDITOR_HEIGHT_PX = 500;

// Cloudflare R2 upload constraints (stricter than local)
export const MAX_VIDEO_FILE_SIZE_R2 = 100 * 1024 * 1024; // 100MB

