Project: Next.js + TypeScript, Drizzle ORM (SQLite/D1), Cloudflare D1 in prod, Better-Sqlite3 in dev. Server-side services under lib/services/server/*. API often via app/api/[[...route]] or specific routes. Use db/schema.ts for Drizzle schema exports. Prefer db.select() with post-processing (no args), per earlier type fixes. Time fields are integer ms epoch (Date.now()). Routing includes /articles/[id] and /profile/[username].