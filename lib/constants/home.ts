export const HOME_DEFAULT_SECTION_LIMIT = 10;
// Trending セクションの再検証間隔（秒）
export const HOME_TRENDING_REVALIDATE_SECONDS = 3600; // 1時間
// Recent は即時反映。UI 側のキャッシュを無効化するためのフラグ。
export const HOME_RECENT_DISABLE_CACHE = true;
